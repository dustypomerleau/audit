use leptos::{component, view, IntoView};

#[component]
pub fn Register() -> impl IntoView {
    view! {
        "register a new surgeon (this form must contain all the elements in default::Surgeon, including SIAs)"
    }
}

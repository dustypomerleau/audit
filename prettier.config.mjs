const config = {
    overrides: [
        { files: "*.md", options: { tabWidth: 2 } },
        { files: "*.svg", options: { parser: "html" } },
    ],
    printWidth: 100,
    tabWidth: 4,
};

export default config;
